[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "vlm-project"
version = "0.1.0"
description = "Turn driving footage into a searchable database of scene descriptions using a vision-language model."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = [
    "nuscenes-devkit>=1.1.11",
    "transformers>=4.38",
    "torch>=2.1",
    "Pillow>=10.0",
    "numpy>=1.24",
]

[project.optional-dependencies]
# Optional raw-video loader.
video = [
    "opencv-python-headless>=4.8",
]
# Optional local Moondream2 backend (instruction-following VLM; heavier than
# BLIP). transformers/torch come from the core deps; these are its extra needs.
moondream = [
    "einops>=0.7",
    "torchvision>=0.16",
]
# Development / test tooling.
dev = [
    "pytest>=8.0",
    "pytest-mock>=3.12",
    "ruff>=0.4",
]

[project.scripts]
vlm-project = "vlm_project.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
# Ship the committed correctness baseline inside the package so verify-accuracy
# finds it wherever the image runs (no volume mount needed).
"vlm_project.baselines" = ["*.json"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "integration: exercises the real BLIP weights / network; skipped by default in CI",
]
# Integration tests are opt-in: run them with `pytest -m integration`.
addopts = "-m 'not integration'"

[tool.ruff]
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
